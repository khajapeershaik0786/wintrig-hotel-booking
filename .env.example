# Expo client configuration
# Point the app at your backend. Use the Render URL for production,
# or http://localhost:4000 when running the API locally.
EXPO_PUBLIC_API_URL=https://wintrig-hotel-booking.onrender.com
